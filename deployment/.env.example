# Isoproxy Configuration
# Copy this file to .env and fill in your values

# Required: Base URL of the upstream Anthropic-compatible API
# Examples:
#   - https://api.anthropic.com (Official Anthropic)
#   - https://your-provider.example.com (Custom provider)
PROXY_UPSTREAM_BASE=https://api.anthropic.com

# Required: API key for upstream authentication
# Must be a valid API key, not a placeholder
PROXY_API_KEY=your-api-key-here

# Optional: Default model to use if not specified in requests
# If set, this will override the model field in all incoming requests
# PROXY_DEFAULT_MODEL=claude-3-5-sonnet-20241022

# Optional: Timeout for upstream requests in seconds (default: 30)
# Valid range: 1-300
# PROXY_TIMEOUT=30

# Optional: Host to bind the proxy server to (default: 127.0.0.1)
# WARNING: Only bind to 127.0.0.1 for security unless you know what you're doing
# PROXY_HOST=127.0.0.1

# Optional: Port to bind the proxy server to (default: 9000)
# PROXY_PORT=9000
