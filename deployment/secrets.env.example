# Isoproxy API Keys and Secrets
#
# This file should be placed at /etc/isoproxy/secrets.env with 600 permissions
# Contains sensitive API keys that should never be committed to version control
#
# SECURITY NOTES:
# - Keep this file separate from config.env for better security
# - Use strict file permissions: sudo chmod 600 /etc/isoproxy/secrets.env
# - Only the isoproxy user should have read access
# - API keys should be rotated periodically

# Anthropic API Key (required if using anthropic provider)
ANTHROPIC_API_KEY=sk-ant-your_anthropic_api_key_here

# Add other provider API keys as needed
# Z_AI_API_KEY=your_z_ai_api_key_here
# MOONSHOT_API_KEY=your_moonshot_api_key_here